Ext.define('JJCon.store.EventsSchedules', {	extend: 'Ext.data.Store',	requires: [		'JJCon.model.EventSchedule'		,'Ext.data.reader.Array'	],			config: {		model: 'JJCon.model.EventSchedule',		autoLoad: true,		grouped: true,		grouper: {			sortProperty: 'startTime',			groupFn: function (record) {				if (record.get('endTime') !== undefined)					return Ext.Date.format(record.get('startTime'), 'g:i A') + "&ndash;" + Ext.Date.format(record.get('endTime'), 'g:i A');							}		},		proxy: {			type: 'ajax',			url: 'schedule.json',			reader: {				type: 'array'			}		}	}});